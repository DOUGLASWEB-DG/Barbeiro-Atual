# BarberOS - Variáveis de Ambiente
# Copie este arquivo para .env e ajuste conforme necessário

# URL do banco de dados SQLite (padrão: arquivo local)
DATABASE_URL="file:./dev.db"

# Porta do servidor (opcional, padrão: 3000)
# PORT=3000
